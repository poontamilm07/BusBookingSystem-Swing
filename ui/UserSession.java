package ui;

public class UserSession {
    // This static variable will hold the ID of the currently logged-in user
    public static int currentUserId = 0; 
    public static String currentUserName = "";
}